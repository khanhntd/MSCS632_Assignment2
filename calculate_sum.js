function calculateSum(arr) {
    let total = o;  // Typo: 'o' instead of '0'
    for (let num of arr) {
        total += num;
    }
    return total;
}

let numbers = [1, 2, 3, 4, 5];
let result = calculate Sum(numbers); // Typo: space between 'calculate' and 'Sum'
console.log("Sum in JavaScript:", result);